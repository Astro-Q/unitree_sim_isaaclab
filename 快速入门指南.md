# 🚀 快速入门指南

> 5分钟快速上手宇树机器人双臂灵巧手抓取项目

---

## 📋 前提条件

确保你已经：
- ✅ 安装了 Isaac Sim 4.5.0 或 5.0.0
- ✅ 安装了项目依赖：`pip install -r requirements.txt`
- ✅ 下载了资产文件：`. fetch_assets.sh`

---

## 🎯 场景1: 运行仿真环境

### 最简单的方式

```bash
python sim_main.py \
  --device cuda \
  --enable_cameras \
  --task Isaac-PickPlace-Cylinder-G129-Inspire-Joint \
  --enable_inspire_dds \
  --robot_type g129
```

**说明**：
- G1机器人（29自由度）
- Inspire 5指灵巧手
- 抓取圆柱体任务

### 查看主视图

启动后，点击：**PerspectiveCamera → Cameras → PerspectiveCamera**

---

## 🎯 场景2: 数据采集和回放

### 数据采集

**前提**：需要安装 [xr_teleoperate](https://github.com/unitreerobotics/xr_teleoperate)

```bash
# 终端1: 启动仿真
python sim_main.py \
  --task Isaac-PickPlace-Cylinder-G129-Inspire-Joint \
  --enable_inspire_dds \
  --robot_type g129

# 终端2: 启动遥操作
cd /path/to/xr_teleoperate
python teleop_main.py --robot g1 --effector inspire --record
```

### 数据回放

```bash
python sim_main.py \
  --task Isaac-PickPlace-Cylinder-G129-Inspire-Joint \
  --enable_inspire_dds \
  --robot_type g129 \
  --replay_data \
  --file_path "/path/to/your/data"
```

---

## 🎯 场景3: 快速训练模型

使用快速训练脚本：

```bash
./scripts/quick_start_training.sh \
  --data ./teleoperate_data \
  --output ./models/my_model \
  --epochs 100
```

这个脚本会自动完成：
1. 数据预处理
2. 模型训练
3. 转换为ONNX格式

---

## 🎯 场景4: 测试训练好的模型

在仿真中测试模型：

```bash
python sim_main.py \
  --task Isaac-PickPlace-Cylinder-G129-Inspire-Joint \
  --enable_inspire_dds \
  --robot_type g129 \
  --action_source policy \
  --model_path ./models/my_model/model.onnx
```

---

## 🎯 场景5: 部署到真实机器人

```bash
python deployment/deploy_to_robot.py \
  --model_path ./models/my_model/model.onnx \
  --robot_type g1 \
  --robot_ip 192.168.123.10 \
  --effector inspire \
  --safety_mode \
  --frequency 50.0
```

⚠️ **注意**：首次部署建议使用测试模式：`--test_mode`

---

## 🎯 场景6: 端到端完整流程

使用端到端脚本一次性完成所有步骤：

```bash
./scripts/end_to_end_workflow.sh
```

脚本会引导你完成：
1. 数据采集
2. 数据增强
3. 数据预处理
4. 模型训练
5. 模型评估
6. 真机部署

---

## 📚 快速命令参考

### 常用任务

```bash
# G1 + Dex1 + 抓取圆柱体
python sim_main.py --task Isaac-PickPlace-Cylinder-G129-Dex1-Joint --enable_dex1_dds --robot_type g129

# G1 + Dex3 + 抓取红色方块
python sim_main.py --task Isaac-PickPlace-RedBlock-G129-Dex3-Joint --enable_dex3_dds --robot_type g129

# G1 + Inspire + 堆叠方块
python sim_main.py --task Isaac-Stack-RgyBlock-G129-Inspire-Joint --enable_inspire_dds --robot_type g129

# H1-2 + Inspire + 抓取圆柱体
python sim_main.py --task Isaac-PickPlace-Cylinder-H12-27dof-Inspire-Joint --enable_inspire_dds --robot_type h1_2
```

### 性能优化参数

```bash
# Headless模式（无GUI，更快）
--headless

# 减少渲染频率
--render_interval 10

# 禁用渲染
--no_render

# 减少相机更新频率
--camera_write_interval 5
```

---

## 🔍 目录导航

完成快速入门后，查看以下文档深入了解：

1. **[项目总览](项目总览.md)** - 项目整体介绍
2. **[完整工作流程指南](docs/完整工作流程指南.md)** - 详细教程
3. **[API文档](docs/API文档.md)** - API参考
4. **[QUICK_REFERENCE](QUICK_REFERENCE.md)** - 命令速查

---

## ❓ 遇到问题？

### 1. 仿真窗口黑屏
点击窗口激活渲染：左键点击仿真窗口

### 2. DDS通信失败
```bash
export CYCLONEDDS_URI='<CycloneDDS><Domain><Id>1</Id></Domain></CycloneDDS>'
```

### 3. GPU内存不足
- 使用 `--device cpu`
- 或减小批次大小 `--batch_size 32`

### 4. 找不到命令
确保脚本有执行权限：
```bash
chmod +x scripts/*.sh
```

### 5. 更多问题
查看 [完整工作流程指南 - 故障排查](docs/完整工作流程指南.md#故障排查)

---

## 🎉 下一步

恭喜！你已经掌握了基本操作。接下来可以：

1. ✨ 尝试不同的任务和机器人组合
2. 📊 采集自己的数据集
3. 🤖 训练自定义模型
4. 🚀 部署到真实机器人
5. 🔧 开发新的任务场景

**祝你使用愉快！** 🎊

---

<div align="center">
  <p>💡 提示：随时查看<a href="QUICK_REFERENCE.md">快速参考</a>获取常用命令</p>
  <p>💬 加入我们的 <a href="https://discord.gg/ZwcVwxv5rq">Discord</a> 社区交流</p>
</div>
